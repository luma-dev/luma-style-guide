require('@rushstack/eslint-patch/modern-module-resolution');

/** @type {import('eslint').parserOptions} */
module.exports = {
  rules: {
    'sort-imports': 'off',
    'sort-keys': 'off',
    'sort-vars': 'off',
  },
};
